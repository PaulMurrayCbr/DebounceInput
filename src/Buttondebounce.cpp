/**
 * ButtonDebounce - a library for debouncing digital inputs.
 * See ButtonDebounce.h for details.
 */

#include "ButtonDebounce.h"

// no code yet, just attempting to get the project structure correct

/*
 
    note to self: even is we don't perform the read because 4ms, we must turn off the 'changed' bit
    after the call to read();
 
*/